
<div class='container'>
  {{#if pairShow}}
  <h2>Have you gone through all the steps on the Learn How to Program debugging lesson?</h2>
  <button class='btn' {{action "showAnotherPair" }}>Yes >></button>


  {{else if anotherPair}}
  <h2>Have you asked another pair for help?</h2>
  <button class='btn' {{action "showFifteenMinutes"}}>Yes >></button>

  {{else if showFifteenMinutes}}
  <h2>Have you spent 15 minutes going through through the problem documenting every step</h2>
  <button class='btn' {{action "showForm"}}>Yes >></button>

  {{else if showForm}}
  <form>
    <br>
    {{textarea value=title class='form-control' id="title" rows="1" cols="50" placeholder='Please enter your name'}}
    <div class="form-group">
      {{textarea value=description class='form-control' id="description" rows="8" cols="40" placeholder='Give a brief description of the issue you are having.'}}
      <button class='btn' id='help'>Help!</button>
    </div>
  </form>
  {{/if}}
</div>


{{yield}}
